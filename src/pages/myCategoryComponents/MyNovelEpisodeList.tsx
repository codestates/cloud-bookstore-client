import React from 'react';
import { withRouter } from 'react-router-dom';

const MyNovelEpisodeList: React.FC = () => {
  return (
    <div>
      <h1> MyWorkEpNovelList</h1>
    </div>
  );
};

export default withRouter(MyNovelEpisodeList);
